val pi = 22 / 7;
println(pi);

val two_pi = 2 * pi;
println(two_pi);

println("Fun fact: " + str(two_pi) + " is a better circle number than pi!");

val fail = fun -> 0 / 0;
// println(fail());

val res = 0xFF + 0x1
println(res)

println(2**4)
println(3**3)
println(0xFF**2)
println(2**4**2)
println(0xFFFFFFFFFFF**2)